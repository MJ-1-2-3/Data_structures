#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

struct link{
    int data;
    struct link  *ptr;

};

int main() {
    // int n;
    // scanf("%d",&n);
    // int* arr = (int*)malloc(n*sizeof(struct link));
    // for(int i=0;i<n;i++)
    // {
    //     scanf("%d",&arr[i].data);
    // }

    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
